extends base
include mixins/video 

block content
  h2 Welcome here you will see the trending videos.
  each video in videos  
    +video(video)  
    //- iterable의 length가 0이면 아래의 else문이 동작함(pug 문법)
  else 
    li Sorry nothing found.